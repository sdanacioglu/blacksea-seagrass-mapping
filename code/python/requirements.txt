# Python dependencies for figure generation scripts (matplotlib-only, no seaborn)
matplotlib>=3.7
pandas>=2.0
numpy>=1.24

# Optional — Word document automation (only if working with .docx outputs)
# python-docx>=1.1
